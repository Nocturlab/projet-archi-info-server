{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "postgres",
            "type": "shell",
            "command": "docker run -it --rm --name postgres -p 5432:5432 postgres:alpine",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}